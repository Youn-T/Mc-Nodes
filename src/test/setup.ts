// ============================================================
// Test setup — loaded before every test file (vitest).
// ============================================================

import '@testing-library/jest-dom';
